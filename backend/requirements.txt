# Runtime dependencies, pinned to exact versions for reproducibility.
# Audited with pip-audit; see scripts/security_scan.sh.
#
# starlette is pinned explicitly rather than left to fastapi's floor: the
# transitive version fastapi accepted carried seven known advisories.
fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
httpx==0.28.1
numpy==2.2.1
pandas==2.2.3
scikit-learn==1.6.0
python-dateutil==2.9.0.post0
