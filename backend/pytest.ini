[pytest]
testpaths = tests
python_files = test_*.py
asyncio_mode = auto
filterwarnings =
    error::DeprecationWarning:app.*
addopts = -q --strict-markers
markers =
    integration: requires a live PostgreSQL database
    network: contacts the real Polymarket API
